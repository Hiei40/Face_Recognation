# Advanced Face Recognition System
## Government Project Edition

نظام متقدم للتعرف على الوجوه باستخدام مكتبة face_recognition (المبنية على dlib) بدقة 99.38%

مناسب للاستخدام في المشاريع الحكومية والأمنية.

---

## ✨ الميزات الرئيسية

- 🎯 **دقة عالية جداً**: 99.38% على LFW benchmark
- 👓 **يتعامل مع التغييرات**: النظارات، الحجاب، تغطية جزئية للوجه
- 💡 **قوي ضد تغيرات الإضاءة**: يعمل في ظروف إضاءة مختلفة
- 📸 **تسجيل متعدد**: تسجيل عدة صور لكل شخص لدقة أفضل
- 📊 **عرض نسبة الثقة**: يعرض نسبة الثقة بالتعرف
- 📝 **سجلات المراجعة**: تسجيل جميع عمليات التعرف (مهم للمؤسسات الحكومية)
- ⚙️ **قابل للتخصيص**: مستويات أمان مختلفة (صارم، عادي، متساهل)

---

## 📋 المتطلبات

- Python 3.8 أو أحدث (يُنصح باستخدام 3.11 أو 3.12)
- كاميرا ويب
- Windows / Linux / macOS

---

## 🚀 التثبيت

### الطريقة 1: التثبيت السريع (موصى بها لـ Windows)

#### الخطوة 1: تثبيت المكتبات الأساسية

```powershell
py -m pip install --upgrade pip
py -m pip install opencv-python numpy pillow
```

#### الخطوة 2: تثبيت dlib (اختر أحد الخيارات)

**الخيار A: باستخدام Wheels الجاهزة (الأسهل)**

```powershell
py -m pip install https://github.com/z-mahmud22/Dlib_Windows_Python3.x/raw/main/dlib-19.24.99-cp312-cp312-win_amd64.whl
```

> **ملاحظة**: غيّر `cp312` حسب إصدار Python لديك:
> - Python 3.12: `cp312`
> - Python 3.11: `cp311`
> - Python 3.10: `cp310`

**الخيار B: باستخدام pip (يتطلب Visual Studio Build Tools)**

```powershell
py -m pip install dlib
```

إذا فشل التثبيت، قم بتثبيت [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022) أولاً.

#### الخطوة 3: تثبيت face_recognition

```powershell
py -m pip install face-recognition
```

### الطريقة 2: التثبيت من requirements.txt

```powershell
py -m pip install -r requirements.txt
```

---

## 📖 دليل الاستخدام

### 1️⃣ عرض الأوامر المتاحة

```powershell
py face_recognition_app.py --help
```

### 2️⃣ تسجيل وجه جديد

**تسجيل بسيط (صورة واحدة أو أكثر حسب الإعدادات):**

```powershell
py face_recognition_app.py --enroll "محمد أحمد"
```

**تسجيل متقدم (عدة صور للدقة العالية):**

```powershell
py face_recognition_app.py --enroll "فاطمة علي" --photos 5
```

> 💡 **نصيحة**: للمشاريع الحكومية، يُنصح بتسجيل **3-5 صور** لكل شخص بزوايا وتعبيرات مختلفة.

### 3. التسجيل الاحترافي (Professional Enrollment)
للحصول على أفضل دقة في الحالات الحكومية (مثل تغطية العين)، يطلب النظام الآن **10 صور**:
`py face_recognition_app.py --enroll "اسمك"`
*   **الصور 1-2**: وجه طبيعي.
*   **الصور 3-4**: تغطية العينين (يركز النظام على الأنف والفم).
*   **الصور 5-6**: تغطية الفم (يركز النظام على العين والأنف).
*   **الصور 7-10**: زوايا جانبية وتغطية أنصاف الوجه.

### 4. ميزة ذاكرة التعرف (Persistence Memory)
تم إضافة نظام ذاكرة ذكي:
*   إذا تعرف النظام عليك ثم غطيت وجهك بالكامل، سيبقى النظام "متذكراً" لهويتك لمدة ثانية كاملة (15 إطاراً).
*   يظهر بجانب الاسم كلمة `(Memory)` للدلالة على أن النظام يستخدم الذاكرة المكانية للتعرف عليك.

**أثناء التسجيل:**
- ضع وجهك بوضوح في الإطار
- اضغط `SPACE` لالتقاط كل صورة
- جرب زوايا مختلفة (وجه مستقيم، جانبي قليلاً، مع/بدون نظارة)
- اضغط `Q` للإلغاء

### 3️⃣ تشغيل نظام التعرف

```powershell
py face_recognition_app.py
```

**ستظهر لك:**
- إطار الكاميرا مع مربعات حول الوجوه المكتشفة
- اسم الشخص ونسبة الثقة بالتعرف
- عدد الوجوه المسجلة
- معدل الإطارات (FPS)

اضغط `Q` للخروج.

### 4️⃣ عرض قائمة الوجوه المسجلة

```powershell
py face_recognition_app.py --list
```

### 5️⃣ حذف وجه مسجل

```powershell
py face_recognition_app.py --delete "اسم الشخص"
```

### 6️⃣ عرض الإعدادات الحالية

```powershell
py face_recognition_app.py --config-info
```

---

## ⚙️ التخصيص والإعدادات

يمكنك تخصيص النظام عبر تعديل ملف `config.py`:

### مستويات الأمان

```python
# في config.py، غيّر:
CURRENT_SECURITY_LEVEL = 'NORMAL'
```

**الخيارات المتاحة:**
- `'STRICT'`: صارم - للاستخدام الحكومي (tolerance: 0.4)
- `'NORMAL'`: عادي - متوازن (tolerance: 0.6)
- `'LENIENT'`: متساهل - لسهولة الوصول (tolerance: 0.7)

### نموذج الكشف

```python
DETECTION_MODEL = 'hog'  # سريع على CPU
# أو
DETECTION_MODEL = 'cnn'  # أدق، يحتاج GPU
```

### الأداء

```python
FRAME_SKIP = 2          # معالجة كل إطار ثاني (أسرع)
DETECTION_SCALE = 0.5   # تصغير للكشف الأسرع
```

---

## 📁 هيكل المشروع

```
face recognation/
├── face_recognition_app.py    # التطبيق الرئيسي (محدّث)
├── config.py                   # ملف الإعدادات (جديد)
├── requirements.txt            # المكتبات المطلوبة (محدّث)
├── README.md                   # هذا الملف (محدّث)
│
├── known_faces/               # مجلد الوجوه المسجلة
│   ├── *.jpg                  # صور الوجوه
│   └── face_encodings.pkl     # بيانات التعرف
│
└── logs/                      # سجلات النظام
    └── recognition_log.txt    # سجل عمليات التعرف
```

---

## 🔧 استكشاف الأخطاء

### مشكلة: dlib لا يثبت على Windows

**الحل:**
1. استخدم wheels الجاهزة من [z-mahmud22/Dlib_Windows_Python3.x](https://github.com/z-mahmud22/Dlib_Windows_Python3.x)
2. أو ثبّت Visual Studio Build Tools

### مشكلة: الكاميرا لا تعمل

**الحل:**
- تأكد من عدم استخدام تطبيق آخر للكاميرا
- جرب تغيير `CAMERA_INDEX` في `config.py` (0, 1, 2, ...)

### مشكلة: بطء في الأداء

**الحلول:**
1. غيّر `DETECTION_MODEL` إلى `'hog'` في `config.py`
2. زد `FRAME_SKIP` إلى 2 أو 3
3. قلل `DETECTION_SCALE` إلى 0.4 أو 0.3

### مشكلة: لا يتعرف على الوجه عند تغطية العينين

**الحلول:**
1. سجّل عدة صور (3-5) بأوضاع مختلفة
2. قلل مستوى الأمان إلى `'LENIENT'`
3. سجّل صورة واحدة مع تغطية العينين

### مشكلة: تعرّف خاطئ (False Positives)

**الحل:**
- زد مستوى الأمان إلى `'STRICT'`
- سجّل المزيد من الصور لكل شخص

---

## 📊 مقارنة الأداء

| الميزة | النظام القديم (Haar Cascade) | النظام الجديد (dlib) |
|--------|------------------------------|---------------------|
| **الدقة** | ~60-70% | **99.38%** ✅ |
| **تغطية العينين** | ❌ لا يعمل | ✅ يعمل |
| **تغيرات الإضاءة** | ⚠️ ضعيف | ✅ قوي |
| **الزوايا المختلفة** | ❌ محدود | ✅ ممتاز |
| **السرعة (CPU)** | سريع | متوسط |
| **الاستخدام الحكومي** | ❌ غير موصى به | ✅ موصى به |

---

## 🔒 إرشادات النشر الحكومي

### التوصيات للاستخدام الرسمي:

1. **مستوى الأمان**: استخدم `STRICT` mode
2. **التسجيل المتعدد**: 3-5 صور لكل شخص على الأقل
3. **السجلات**: فعّل `ENABLE_LOGGING = True`
4. **النسخ الاحتياطي**: انسخ `known_faces/` و `logs/` بشكل دوري
5. **الأمان**: احمِ ملف `face_encodings.pkl` (يحتوي بيانات حساسة)
6. **الأداء**: استخدم جهاز بـ GPU للمعالجة السريعة

### معايير القبول:

- ✅ دقة التعرف > 95% في الظروف الطبيعية
- ✅ عدم وجود تعرّف خاطئ (False Positives)
- ✅ سرعة معالجة مقبولة (1-5 ثوانية للإطار)
- ✅ سجلات كاملة لجميع العمليات

---

## 📚 موارد إضافية

- [face_recognition Documentation](https://github.com/ageitgey/face_recognition)
- [dlib Documentation](http://dlib.net/)
- [OpenCV Documentation](https://docs.opencv.org/)

---

## 🆘 الدعم

إذا واجهت أي مشاكل:

1. راجع قسم **استكشاف الأخطاء** أعلاه
2. تحقق من ملف `logs/recognition_log.txt` للتفاصيل
3. تأكد من تثبيت جميع المكتبات بشكل صحيح

---

## 📄 الترخيص

هذا المشروع للاستخدام الحكومي. تأكد من الامتثال للوائح حماية البيانات المحلية.

---

**تم التطوير بواسطة**: Antigravity AI  
**التاريخ**: 2025-12-24  
**الإصدار**: 2.0 (Government Edition)
